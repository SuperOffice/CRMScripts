#setLanguageLevel 3;
#include "lib-chat";

/*
 * Example on how lib-chat is used.
 */

Integer ejUserId = 7;

//Set chatagent as present
setChatAgentStatus(ejUserId, ENUM_CHAT_STATUS_PRESENT);

//Set chatagent as away
setChatAgentStatus(ejUserId, ENUM_CHAT_STATUS_AWAY);